/**
 * 
 */

function loginFail() {
	swal("로그인 후 사용 가능한 메뉴입니다.");
	return false;
	
	$("#modalBody").html("입력하신 정보가 올바르지 않습니다.");
	$("#myModal").modal("show");
}


function signUp() {
	$("#modalBody2").html("회원가입이 완료되었습니다.");
	$("#myModal2").modal("show");

}