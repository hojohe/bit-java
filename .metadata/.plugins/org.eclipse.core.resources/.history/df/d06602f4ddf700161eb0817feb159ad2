package annotation.quiz;

import java.lang.reflect.Method;
import java.util.Scanner;

import annotation.exam04.RunAnno2;

public class Test {
	public static void main(String[] args) {
		
		Class<?> clz = BoardController.class;
		
		Method[] mArr = clz.getDeclaredMethods();
		for(Method m: mArr) {
			RequestMapping rMpp = m.getAnnotation(RequestMapping.class);
			if(anno == null) continue;
			
			System.out.println("메서드명 : " + m.getName());
			System.out.println("ID : " + anno.id());
			System.out.println("MSG : " + anno.msg());
		}
		
		System.out.println(rMapp);
		Scanner sc = new Scanner(System.in);
		
		/*while (true) {
			System.out.print("경로 : ");
			String url = sc.nextLine();
			if (url.equals("quit")) break;
			
			if(.equals(url)) {
				System.out.println("1");
			}
			
		}*/
	}
}
