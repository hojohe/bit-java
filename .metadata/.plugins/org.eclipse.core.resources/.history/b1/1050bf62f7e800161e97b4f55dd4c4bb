<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script>

	// MyMap 객체 구현하기
	var i = 0;
	function funcLength(key) {
		if(key == "del") {
			i = i - 1;			
		}
		if(key == "put") {
			i = i + 1;			
		}
		return i;
	}
	
	function funcClear() {
		MyMap = new Object();
	}
	
	var MyMap = {
		put: function(key, value) {
			if(key == "id") {
				this.id = value;
			}
			if(key == "name") {
				this.name = value;
			}
			funcLength("put");
		},
		size: function() {
			return funcLength();
		},
		get: function(id) {
			return this.id;
		},
		del: function(key) {
			
			if(key == "id") {
				delete this.id;
			}
			if(key == "name") {
				delete this.name;
			}
			funcLength("del");
		},
		clear: function() {
			this = 
			funcClear();
		}
	}; 
	
	var mData = MyMap;
	mData.put("id", "sbc");
	mData.put("name", "sonbyungchan");
	
	console.log(mData.size());      // 2
	console.log(mData.get("id"));   // sbc
	mData.del("id");          
	console.log(mData.get("id"));   // undefined	
	mData.clear();
	console.log(mData.size());      // 0
</script>
</head>
<body>

</body>
</html>