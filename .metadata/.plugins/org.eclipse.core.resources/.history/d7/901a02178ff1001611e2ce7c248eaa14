package kr.co.mini5.feed.servlet;

import java.io.IOException;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import kr.co.mini5.feed.dao.FeedDAO;
import kr.co.mini5.vo.FeedVO;

@WebServlet("/feed/feedUpdateForm")
public class FeedUpdateFormServlet extends HttpServlet {
	private FeedDAO dao;
	
	public FeedUpdateFormServlet() {
		this.dao = new FeedDAO();
	}

	protected void service(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		FeedVO feed = dao.selectOneFeed(Integer.parseInt(request.getParameter("feedNo")));
		
		System.out.println("시작날짜 : " + feed.getStartDate());		
		System.out.println("알람일자 : " + feed.getAlertDate());		
		System.out.println("매일주간 : " + feed.getAlertSchedule());
		
		request.setAttribute("feed", feed);
		
		RequestDispatcher rd = request.getRequestDispatcher("/jsp/view/feed/feedUpdateForm.jsp");
		rd.forward(request, response);
	}
	
	
	
}
