package kr.co.mini5.feed.servlet;

import java.io.IOException;
import java.util.List;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import kr.co.mini5.feed.dao.FeedDAO;
import kr.co.mini5.vo.FeedVO;

@WebServlet("/feed/feedList")
public class FeedListServlet extends HttpServlet {
	private FeedDAO dao;
	
	public FeedListServlet() {
		this.dao= new FeedDAO();
	}

	protected void service(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		int userNo = Integer.parseInt(request.getParameter("userNo"));
		Integer feedType = request.getParameter("feedType") != null ? Integer.parseInt(request.getParameter("feedType")) : null;
		
		
		System.out.println("1" + userNo);
		System.out.println("2" + feedType);
		System.out.println("3" + request.getParameter("feedType"));
		
		FeedVO feed = new FeedVO();
		feed.setUserNo(userNo);
		
		if(feedType != )
		feed.setFeedType(feedType);
		
		List<FeedVO> feedList = dao.selectList(feed);
		request.setAttribute("feedList", feedList);
		RequestDispatcher rd = request.getRequestDispatcher("/jsp/view/feed/feedList.jsp");
		rd.forward(request, response);
	}
	
}
